import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#1ac334",
    },
    secondary: {
      main: "#3f51b5",
    },
  },
});

export default theme;